<!-- res/drawable/tborder.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
   <!-- Background color (white) -->
    <stroke
        android:width="2dp"
        android:color="#908B8B"/> <!-- Border color (black) -->
    <padding
        android:left="10dp"
        android:top="25dp"
        android:right="16dp"
        android:bottom="25dp"/>
    <gradient android:startColor="@color/transparent"
             android:endColor="@color/transparent"/>


</shape>